# Copy to `.env` in this directory so docker compose picks it up:
#   cp .env.docker.example .env
#
MYSQL_ROOT_PASSWORD=nice_insurance_root
# Optional: avoid conflicting with MySQL installed on your host
# MYSQL_PUBLISH_PORT=3307
# WEB_PUBLISH_PORT=8080
